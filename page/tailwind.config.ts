
import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      colors: {
        'custom-dark': '#131414', // Define custom color
      },
    },
  },
  plugins: [],
};

export default config;
